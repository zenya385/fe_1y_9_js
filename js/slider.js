$(document).ready(function () {
  $('.slider-team').slick({
    //     // setting-name: setting-value
    dots: true, // крапки
    infinite: true,
    speed: 300,
    arrows: true, // стрілки
    slidesToShow: 3,
    slidesToScroll: 3,
    adaptiveHeight: true, // адаптивна висота слайдера
    centerMode: true,
    // variableWidth: true,
    // centerPadding: '10px',
    // rows:1, //
    // slidesPerRow:
    // easing: 'linear',
    autoplay: true,
    // autoplaySpeed: 2000,
    // pauseOnDotsHover: true,
    // pauseOnHover: true,
    // pauseOnFocus: true,
    // draggable: false,
    // swipe:false,
  });
});

// https://www.youtube.com/watch?v=rXNfDfqtM3M
